{% extends 'base.html.twig' %}

{% block title %}FaqCategory{% endblock %}

{% block body %}
    <h1>FaqCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ faq_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ faq_category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ faq_category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('faq_category_index') }}">back to list</a>

    <a href="{{ path('faq_category_edit', {'id': faq_category.id}) }}">edit</a>

    {{ include('faq_category/_delete_form.html.twig') }}
{% endblock %}
